name: 'slack action'
description: 'Send a message to a Slack channel'
inputs:
  payload:
    description: 'message payload'
    required: true
  token:
    description: 'Slack bot token'
    required: true
runs:
  using: 'node20'
  main: 'dist/index.js'
